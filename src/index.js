import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import reportWebVitals from './reportWebVitals'; // Only one import needed

ReactDOM.render(
  <React.StrictMode>
    {/* Your components go here */}
  </React.StrictMode>,
  document.getElementById('root')
);

// Optionally, measure performance
reportWebVitals();
